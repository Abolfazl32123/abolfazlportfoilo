# Abolfazl Rajabi — About Section

Cloudflare Workers Static Assets project matching the supplied About-section design.

## Deploy
npm install
npm run deploy

Put your real resume PDF at `public/resume.pdf` before deploying.
